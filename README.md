# Cake — v0.39

A **light** checkbox on the end of the 30/45/60 bar shows or hides the lighting panel, so the cake
can be seen clearly after tuning. The setting persists, so the panel stays out of the way across
reloads once you've found the numbers you want. Dev overlay only (`?dev=1`).
